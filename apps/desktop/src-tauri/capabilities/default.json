{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the editor main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
