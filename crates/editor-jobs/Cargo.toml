[package]
name = "editor-jobs"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
editor-domain = { path = "../editor-domain" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
